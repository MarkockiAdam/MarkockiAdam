# Full toolchain -- needed only when regenerating the portrait locally.
# The GitHub Action installs requirements-ci.txt instead, which is far lighter.
requests==2.32.3
beautifulsoup4==4.12.3
pillow>=11,<13
numpy>=2,<3
rembg>=2.0.59
onnxruntime>=1.17
